# foxworth-uni Homebrew Tap

Internal Homebrew tap for foxworth-uni projects with precompiled bottles for fast installation.

## Installation

Install formulae directly (Homebrew auto-taps):

```bash
brew install foxworth-uni/tap/danny
```

Or tap first:

```bash
brew tap foxworth-uni/tap
brew install danny
```